services:
  app:
    image: docker.io/rebelein/stundenzettel:latest
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NEXT_PUBLIC_SUPABASE_URL=http://host.docker.internal:8000
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=dein_echter_api_key